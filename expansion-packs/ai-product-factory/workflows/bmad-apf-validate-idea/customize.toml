[workflow]

activation_steps_prepend = []
activation_steps_append = []

persistent_facts = [
  "Validate before building — no code until GO or CONDITIONAL GO.",
  "Every score must cite evidence, not LLM intuition.",
  "file:{project-root}/expansion-packs/ai-product-factory/knowledge/yc-startup-playbook.md",
]

on_complete = "Offer next step: full launch pipeline (bmad-apf-launch-startup) or deep market research (bmad-apf-market-research agent)."
