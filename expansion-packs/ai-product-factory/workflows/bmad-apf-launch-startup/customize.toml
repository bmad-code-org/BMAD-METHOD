[workflow]

activation_steps_prepend = [
  "Load file:{project-root}/expansion-packs/ai-product-factory/knowledge/product-pipeline.md",
  "Load file:{project-root}/expansion-packs/ai-product-factory/checklists/launch-checklist.md",
]

activation_steps_append = []

persistent_facts = [
  "This is the master orchestrator workflow — idea to production-ready startup.",
  "Every phase must produce deterministic artifact files before proceeding.",
  "Delegate code implementation to Cursor; keep planning in BMAD.",
  "Reuse BMM workflows (bmad-prd, bmad-create-architecture, bmad-dev-story) instead of reinventing.",
]

on_complete = """
Summarize the launch:
1. List all artifact paths produced across phases
2. List all deployed URLs (app, landing, stores)
3. Present the launch checklist completion status
4. Recommend first growth experiments via bmad-apf-experiment
5. Offer bmad-apf-iterate-product for post-launch iteration
"""
