# Module Setup

This standalone module does not require interactive setup prompts.

On install:
- Register module metadata from `assets/module.yaml`
- Register help entries from `assets/module-help.csv`
- Install `bmad-orchestrator` skill and its `customize.toml` agent metadata

No additional user inputs are required.