# Prevent peer dependency warnings during installation
legacy-peer-deps=true

# Improve install performance
prefer-offline=true

# Require packages to be at least 7 days old before resolving (protects against malicious newly published packages)
min-release-age=7
