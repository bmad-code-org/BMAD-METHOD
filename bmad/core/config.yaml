# BMAD Core Configuration
# Generated: 2025-10-22 - Fixed missing required fields

# Core Identity
user_name: "Hamilton Bailey"
communication_language: "en-AU"
output_folder: "/Users/hbl/Documents/BMAD-METHOD/bmad/output"

# Auto-execution
auto_continue_steps: true # In party mode, agents work autonomously

# Party Mode Configuration
party_mode:
  manifest: "{project-root}/bmad/_cfg/agent-manifest.csv"
  agent_overrides: "{project-root}/bmad/_cfg/agents/*.customize.yaml"
  output_folder: "{project-root}/bmad/core/workflows/party-mode/output"
